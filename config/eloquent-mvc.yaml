application:
  root: <% @var user-homedir-pathname %>src/cl/LESTClient/
log:
  directory: <% @var user-homedir-pathname %>src/cl/LESTClient/log/
server:
  port: 8087
  server: hunchentoot
static-file:
  prefix: /static/
